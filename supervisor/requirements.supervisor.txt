# NSCLC Supervisor — FastAPI + Strands + Bedrock + local drug_library
# Python 3.11 (Dockerfile.supervisor)

# Web server
fastapi==0.115.4
uvicorn[standard]==0.32.0
sse-starlette==2.1.3
pydantic>=2.11.0,<3.0.0

# AWS
boto3==1.42.77
botocore==1.42.91
s3fs==2026.3.0
aiobotocore==3.5.0
typing_extensions>=4.13.2,<5.0.0

# Data
pandas==2.3.2
pyarrow==17.0.0
numpy==1.26.4

# Strands agent SDK
strands-agents==1.36.0

# rdkit — drug_library_lambda local invoke용 (Lambda 250MB 한도 회피)
rdkit==2024.3.2

# requests (외부 PubMed 호출 등에 쓰일 수 있음)
requests==2.32.3
